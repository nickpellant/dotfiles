#!/bin/sh
#
# bootstrap-macos
#
# `boostrap-macos` installs hard dependencies for macos dotfiles

BASEDIR=$(dirname $0)

echo "Bootstrapping macOS..."

$BASEDIR/install-homebrew
$BASEDIR/configure-macos
$BASEDIR/bundle-macos

echo "Bootstrapping complete."
