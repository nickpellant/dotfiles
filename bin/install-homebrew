#!/bin/bash
#
# install-homebrew
#
# `install-homebrew` installs Homebrew on macOS & Linux

if test ! "$(which brew)"
then
    echo "    ›› Installing Homebrew..."

    ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
else
  echo "    ›› Homebrew already installed, skipping..."
fi

exit 0
