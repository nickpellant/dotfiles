#!/bin/bash
#
# bundle-dev
#
# `bundle-dev` bundles Brewfile.dev

DOTFILESDIR="$(dirname "$0")"/..

echo "    ›› Bundling Brewfile.dev..."

brew bundle --file="$DOTFILESDIR"/Brewfile.dev | sed 's/^/      /'