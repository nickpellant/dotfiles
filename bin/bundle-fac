#!/bin/bash
#
# bundle-fac
#
# `bundle-fac` bundles Brewfile.fac

DOTFILESDIR="$(dirname "$0")"/..

echo "  ›› Bundling Brewfile.fac..."

brew bundle --file="$DOTFILESDIR"/Brewfile.fac | sed 's/^/      /'
