#!/bin/bash
#
# provision-battlestation
#
# `provision-battlestation` provisions battlestation

BASEDIR=$(dirname "$0")

echo "Provisioning battlestation..."

"$BASEDIR"/bootstrap-macos
"$BASEDIR"/bootstrap-dev

echo "Provisioning complete."
